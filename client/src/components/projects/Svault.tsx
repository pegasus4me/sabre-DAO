type TSvault = {
  logo: string;
  category: string;
  name: string;
  statut: string;
  description: string;
  price: string;
  round: string;
  maxCap: string;
  viewMore: () => void;
};

export default function Svault({
  logo,
  category,
  name,
  statut,
  description,
  price,
  round,
  maxCap,
  viewMore,
} : TSvault): JSX.Element {
  return <article>
    
  </article>;
}
